[general]
description = {{ data.description }}
calculation_mode = {{ data.preparationCalculationMode }}
{% if data.aggregate_by %}
aggregate_by = {{ data.aggregate_by }}
{% endif %}

[exposure]
exposure_file = exposure.xml

[vulnerability]
structural_vulnerability_file = vulnerability.xml